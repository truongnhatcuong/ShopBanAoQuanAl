import { getCookie } from "cookies-next";
import Header from "./components/Header";
import cookie from "cookie";

const Page = () => {
  return (
    <div className="">
      <div>
        <Header />
      </div>
      <div>{}</div>
    </div>
  );
};

export default Page;
